# Used by docker compose. Generate your own 32+ character value before putting
# a public instance online. Do not commit a real production secret.
JWT_SECRET=replace-with-a-long-random-production-secret
