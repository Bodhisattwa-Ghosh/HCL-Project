# Leave blank to proxy through the same origin. Set this to the service origin in production.
# The app appends /api automatically, e.g. http://localhost:8080 becomes http://localhost:8080/api.
VITE_API_URL=http://localhost:8080
